# Copy this to server/.env for local development
# On Render, set these as Environment Variables in the dashboard

PORT=3001
JWT_SECRET=your_super_secret_key_change_this
NODE_ENV=development

# Database path (relative to server root or absolute)
DB_PATH=./server/data/smartroad.db

# Upload directory (absolute or relative to server root)
UPLOAD_DIR=./server/uploads/imagery

# Comma-separated allowed frontend origins
CORS_ORIGIN=http://localhost:5173,http://localhost:5174

# Superadmin Credentials
SUPERADMIN_USERNAME=superadmin
SUPERADMIN_EMAIL=super@smartroad.gov
SUPERADMIN_PASSWORD=super123
SUPERADMIN_FULLNAME=Super Administrator
